name: databike
channels:
  - conda-forge

dependencies:
  - python==3.8
  - cmake
  - snappy
  - libutf8proc
  - boost
  - boost-cpp
  - thrift-cpp
  - pybind11

  # For tcx file parsing
  - defusedxml

  # For notebook tests
  - nodejs
  - jupyterlab
  - pandas
  - openjdk
  - pip
  - pip:
    - pyarrow
    - pyspark
